# Supabase (required for /join/:id – fetch challenge name from goal_lists)
SUPABASE_URL=https://your-project.supabase.co
# Use service key if RLS blocks anon; otherwise anon key is enough
SUPABASE_SERVICE_KEY=your_service_role_key_here
# SUPABASE_ANON_KEY=your_anon_key_here

# Optional: override base URL when not on Vercel
# BTTRSITE_BASE_URL=https://bttrsite.vercel.app
